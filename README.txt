Dependencies:
    express --save (save adds to package)
    nodemon --save-dev (dev for development only)
    body-parser --save
    mongodb --save
    ejs --save

Variables:

    HTML:
        update button
        print list
    Javascript:
        POST, GET, PUT, DELETE

Instructions:

1. navigate to folder in terminal and type 'npm init'. 
    creates a package.json file

2. npm install express --save

3. npm install body-parser --save (this is middleware to understand request input data)

4. npm install mongodb --save

5. Create a MongoDB project

6. db user:pass = clericmt: St4rw4rs

7. npm install ejs --save

8. mkdir 'views', new file 'index.ejs'